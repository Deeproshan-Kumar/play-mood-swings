// X/Twitter's summary_large_image card is the same 1200×630 shape as the Open
// Graph card, so it reuses the same image rather than duplicating the layout.
export { default, alt, size, contentType } from "./opengraph-image";
